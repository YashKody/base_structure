import 'package:flutter/material.dart';

class SplashPageWeb extends StatelessWidget {
  const SplashPageWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}